# 更新日志

[English](./CHANGELOG.md) | 简体中文

LLM Space 的所有重要变更都会记录在这里。本项目遵循
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 和
[语义化版本](https://semver.org/lang/zh-CN/spec/v2.0.0.html)。

## [4.10.0] - 2026-08-09

此版本加入了“先预览、后应用”的渐进式对话压缩，让长对话更容易导航，并为主要对话工作流补充了更完整的中英文文档入口。

### 新增

- 对话可以将较早轮次压缩成结构化且可持续更新的检查点，同时原样保留可配置数量的最近轮次。压缩会先渲染提示词变量再生成摘要，支持保存每个对话专属的复用指令，并在应用前提供预览；应用时会创建带编号的 `-compact-N.json` 克隆，而不会覆盖原对话。
- 用户和助手消息新增导航锚点，便于在长对话中快速跳转。
- 新增完整的 Atlas Plugin 示例，演示多个 Skills、MCP 服务器、模型提供商、Plugin Tools、Commands、Thread Storages 和 Settings 字段。

### 变更

- 压缩、分享、生成项目和变量对话框新增本地化 Help 入口，分别链接到新建或扩充后的中英文指南。
- 用户手册、快速入门、核心概念和仓库指南现在会链接到分享、项目生成、压缩和变量文档。
- 内置 pi 包升级至 `0.84.1`。

### 修复

- HTML 预览中的内嵌脚本现在可以按预期运行。
- Bash 工具会从所属工作区根目录启动，而不是无关的进程目录。
- 消息导航控件会保持在菜单和对话框下层，不再遮挡更高优先级的界面。

## [4.9.0] - 2026-08-07

此版本加入 Seedream 图片生成和提供商连接配置，提升本地 Plugin Command 的执行透明度，并让 Plugin 与 MCP 设置界面更清晰、更易管理。

### 新增

- 火山方舟提供商可配置 Seedream 图片模型，并通过新的内置 `generate_image` Tool 使用提供商凭据生成图片。
- 模型提供商支持多个具名连接配置，模型选择、连接测试、流式输出和内置 Tool 执行都会识别当前配置。
- Plugin Command 执行时可发布进度、成功和失败反馈，命令面板与桌面运行时共享统一生命周期处理。
- Plugin 设置将 Commands、Tools 和 MCP Servers 按类型分组为可展开区域，并显示对应图标和扩展说明。
- MCP 设置会显示每个内置服务器所属的 Plugin，并缓存 Plugin 工具发现结果，避免重复执行子进程。

### 变更

- Plugins、MCP Servers 和 Remote Servers 使用统一的动态空状态布局，并优化选择态、对比度、间距和插图表现。
- 新手引导减少了图片体积，并改善布局和交互细节。
- 桌面端和 Web 端升级至 Vite 8，同时更新 React、Tailwind CSS、CodeMirror、MCP SDK 和 UI 依赖。
- Plugin 开发文档新增 Command 反馈 API 说明。

### 修复

- 打开分享深链时，会先可靠地激活桌面窗口再跳转到对应内容。
- Plugin 插图会保留图标原始形状，较长的扩展说明也不会溢出设置行。

## [4.8.5] - 2026-08-06

此补丁版本恢复了打包桌面应用中兼容 OpenAI Responses 的提供商在工具调用后的继续执行能力。

### 修复

- 回放工具结果时会使用提供商原始 call ID，并移除内部 response-item 后缀，避免出现 `No tool call found for tool output` 错误。
- 本地 `pi-ai` 补丁现在会在全新安装依赖时正确应用，使开发环境与发布构建保持一致。

## [4.8.4] - 2026-08-05

此版本将本地 Plugin 扩展为一等 Agent 能力，并更新了最重要的初始化、分享和项目生成体验。

### 新增

- 本地 Plugin 可以提供可执行 Tools。用户可将其添加到对话，模型也能像调用内置 Tools 一样调用它们；同时支持稳定身份、包图标、生命周期隔离、结构化结果和所属对话的变量上下文。
- Plugin Commands 支持带引号的命令面板参数，并可通过显式上下文 API 读取当前标签页、文件名和对话。
- 中英文 Plugin 开发指南新增 Commands 与 Tools 文档，涵盖发现、上下文、变量、本地运行限制和错误行为。

### 变更

- 新建对话图库、可运行 Agent 生成器和对话分享流程采用更清晰聚焦的布局，并优化浅色与深色外观。
- Account、Plugins、Skills、Models、MCP Servers 和 Remote Servers 设置拥有更明确的导航、有目的的空状态和一致的列表标题。
- 工作目录控制、Plugin 发现和 Tool 选择提供更直接的反馈与操作，包括在 Finder 中显示已有文件夹。

### 修复

- 打开本地 Plugins 文件夹时改用原生文件系统显示能力，不再执行不可用的 shell 命令。
- 即使切换标签页或并发执行多个调用，Plugin Tool 仍会绑定到拥有该调用的对话。
- 远程运行时和 LangGraph 导出会明确拒绝不支持的本地 Plugin Tools，而不是生成不可用结果。

## [4.8.3] - 2026-08-05

此补丁版本简化了 macOS 上生成 LangGraph 项目后的启动流程。

### 变更

- 用户明确创建生成项目的 `.env` 后，macOS 会打开 Terminal 窗口并执行 `make dev` 启动开发服务器。
- 其他平台、取消配置或启动失败时，仍只会显示生成的项目而不会启动进程。

### 修复

- 含空格或 shell 特殊字符的项目路径会安全传递给 Terminal，且只允许启动用户授权的生成器目录。

## [4.8.2] - 2026-08-05

此补丁版本让文件系统工具可靠处理相对主目录路径，并改善生成后的 LangGraph 开发流程。

### 新增

- 生成的 LangGraph 项目包含 `Makefile`，可运行 `make dev` 启动 `uv run langgraph dev`。

### 变更

- 文件系统 Tool schema 与生成的 Python 实现会一致地说明并展开以 `~/` 开头的路径。
- 桌面运行时和生成的 LangGraph 项目都会展开 MCP stdio 命令与工作目录中的相对主目录路径。

### 修复

- `write`、`edit`、`read`、`ls`、`tree`、`grep`、`glob` 和 `present_files` 不再把 `~/...` 解释为相对桌面应用包的路径。
- 工具调用产生的相对主目录产物路径现在可以正常显示，包括 `glob` 的目标目录。

## [4.8.1] - 2026-08-05

此补丁版本优化了设置页面布局，并让 Plugin 文档更容易访问。

### 变更

- 设置页面采用一致的标题分隔线、内容间距和简洁说明，同时保留紧凑表单行原有的密度。
- Experimental 设置与 Network 设置使用相同的带标签开关布局。
- Plugin 设置可直接跳转到 Plugin 开发指南。

### 修复

- Remote Servers 内容与标题正确对齐，其他分栏设置页面仍保留正确的顶部间距。

## [4.8.0] - 2026-08-05

此版本引入可信本地 Plugins，可通过 Agent 能力、命令、模型集成和自定义对话存储扩展 LLM Space。

### 新增

- 从 `LLM_SPACE_HOME/plugins/` 发现可信本地 Plugins，支持元数据、设置、生命周期控制、兼容性检查和隔离诊断。
- Plugins 可提供 Skills、MCP servers、model providers、Command Palette commands 和 Thread Storages。
- Plugin Skills 可用于本地和远程 Agent 运行时，并可按单个 Skill 或整个 Plugin 启用，无需修改 Plugin 文件。
- 新增中英文 Plugin 开发指南，涵盖包结构、Extensions、设置、生命周期、诊断和示例。

### 变更

- 设置导航按 App、Agent 和 Connections 分组，并为 Remote Servers、MCP Servers 和 Web Search 使用更清晰的名称。
- 设置会分别标识 Plugin 提供的 MCP servers 和 Skills，Plugin 诊断也能显示每个 Extension 的来源。
- 导入的对话文件夹在工作区树中采用独立视觉样式。

### 修复

- 对使用 Responses API 的非 OpenAI 提供商，回放的工具调用 ID 现在会正确规范化。
- Plugin Skill 冲突会标识所有涉及的文件，较长的诊断路径也不会溢出 Plugin 设置面板。

## [4.7.1] - 2026-08-04

此版本加入提供商托管工具，并允许配置 MCP 工具命名方式。

### 新增

- 可配置由提供商托管的工具（包括提供商原生搜索），并在对话中显示其活动与引用。
- 可选择使用 MCP 工具的原始名称，不再强制添加 `mcp__{serverName}__` 前缀。

## [4.7.0] - 2026-08-03

远程运行时经过全面正确性改进：每项对话操作都绑定到所属运行时；此外新增 DeerFlow 运行事件导入，并修复两项安全问题。

### 新增

- 可将 DeerFlow run-event JSONL 文件导入为对话。

### 变更

- 分享、提示词文件解析和辅助生成都会绑定到对话所属运行时，不再回退到本地运行时。
- 切换运行时会保留对话状态，保留的运行时面板也不会再进行不必要的重新渲染。

### 修复

- SSH 信任会绑定到已批准的主机密钥，密钥变化后不再静默通过验证。
- 运行时 bearer token 不再通过 `argv` 传递，避免被其他本地进程读取。
- 过期端口恢复会识别所有权并等待异步清理完成，避免回收仍被其他运行时占用的端口。
- 远程流失败后会正常终止，不再让对话卡住。
- 取消 GitHub Device Flow 验证后会彻底停止，后台尝试不能再自行完成。
- 禁用按钮会根据状态正确应用弱化样式。

## [4.6.3] - 2026-07-31

维护版本：改善剪贴板和工具响应，更新 MiniMax 模型，并提升流式界面的稳定性。

### 新增

- 可从对话标签页的上下文菜单直接将文件引用复制到系统剪贴板。
- 工具响应现在可以携带图片内容。

### 变更

- MiniMax 提供商改用兼容 OpenAI 的 Chat endpoint。MiniMax-M3 更新价格、100 万 token 上下文窗口、文本/图片/视频输入以及自适应或禁用思考；MiniMax-M2.7 仍为始终启用推理的模型。
- Codex CLI 支持 API key 模式，并加入内联运行验证说明和更明确的桌面启动顺序。

### 修复

- `todo_write` 在流式输出时保持稳定：预览不会显示未完成条目，取消状态会保留，且其解析逻辑与通用工具调用渲染隔离。

## [4.6.2] - 2026-07-29

一个改善工具调用检查体验的小版本。

### 新增

- 工具调用条目新增预览按钮，可在单一视图中以格式化 JSON 查看所有参数；原有的“Copy arguments”操作仍然保留。

## [4.6.1] - 2026-07-28

此补丁版本让远程服务器配置变更更安全、更可预测。

### 修复

- 已连接或正在等待主机密钥确认的服务器必须断开后才能编辑或移除，避免意外拆除活跃的远程工作区。
- 移除已断开或失败的服务器后，连接状态会正确更新，并干净地选择可用备用服务器。

## [4.6.0] - 2026-07-27

现在可以通过 SSH 在远程机器运行 LLM Space Agent 并管理其工作区，同时保留本地桌面体验。

### 新增

- **SSH 远程运行时。** 可在设置中连接远程服务器，安装并启动匹配的无头 LLM Space 运行时，并从桌面应用处理远程对话、文件、模型、工具、Skills、Trace 和生成项目。
- 远程运行时状态、主机密钥验证、连接进度、包传输降级、过期进程恢复和按运行时隔离的标签页，让远程会话保持可见且相互隔离。
- 发布构建会在桌面安装包之外同步发布签名后的远程运行时服务器产物。

### 变更

- 共享运行时服务迁移到可复用的 `@llm-space/runtime` 包，由桌面应用和新的无头服务器共同使用。
- 分享的对话会保留可解析的模型配置：可用时使用保存的模型，否则按可预测方式回退。

### 修复

- Skill 发现和读取支持多行 YAML literal 描述；生成的 Python Agent 也保持相同的 frontmatter 行为。

## [4.4.5] - 2026-07-24

响应性能现在一目了然，同时提供更安全的文件系统操作，以及对话运行期间更可预测的控件状态。

### 新增

- 助手消息会显示总响应时间、首 token 时间、生成吞吐率和详细 token 构成。
- Prompt 目录变量会验证配置目录是否存在。

### 变更

- 消息统计可在耗时和 token 摘要间切换，并会跨会话记住偏好视图。
- 打开文件、目录、生成项目和 Skill 位置统一使用经过验证的文件系统操作，并提供更清晰的失败提示。
- 对话运行时，会禁用可能修改或导出对话的顶部操作；运行设置仍可在 Stop 按钮旁访问。
- 更新模型运行时依赖，并将 Ark 最大输出 token 设置与提供商的 128,000 token 限制对齐。

### 修复

- `ask_user_question` 始终会终止当前 Agent 轮次，包括较旧的已保存对话，确保 ReAct 模式不会在用户回答前继续执行。

## [4.4.4] - 2026-07-23

维护版本：让本地 lint 验证在发布前与 CI 保持一致。

### 变更

- Lint 遇到任何 warning 都会失败，发布检查清单要求 lint 以零 warning、零 error 干净通过。

### 修复

- ESLint 在 Bun 和 Node 下都会将 `bun:test` 一致识别为内置模块，避免因环境差异导致 import 顺序失败。
- ESLint 配置使用显式 ESM 扩展名，消除 Node 的模块类型 warning。

## [4.4.3] - 2026-07-23

专门修复生成的 LangGraph 项目使用工作区感知提示词模板的问题。

### 修复

- 生成的 Python 提示词运行时会向 system 和 meta user prompts 提供内置 `current_working_directory` 变量。
- 生成项目支持 LLM Space 的 `@include(...)` 宏，包括递归渲染、文件缺失、无效嵌套模板和深度保护。

## [4.4.2] - 2026-07-23

提示词模板维护版本，聚焦工作区感知 Agent、条件文件包含和更可预测的 Skill 选择。

### 新增

- **当前工作目录变量。** 对话新增内置 `current_working_directory` 变量，可手动编辑或使用原生目录选择器。General Agent 会用它描述工作区。
- **条件文件包含。** 提示词模板可以调用 `exists(path)` 判断 `AGENTS.md` 等可选文件是否存在。
- 可直接从 Skills 设置页面打开 Skill 目录。

### 变更

- Skill 选择会区分“包含所有已启用 Skills”和“有意不选择任何 Skill”。

### 修复

- 当包含的文本本身不是有效模板时，递归 `@include(...)` 渲染仍会保留可读的文件内容。
- include 渲染语义变化时会使旧快照失效，避免修复后仍沿用过期的空输出。

## [4.4.1] - 2026-07-22

维护版本：重点支持将 meta user prompt 带入生成的 LangGraph 项目，并修复实时 Skills 和项目初始化问题。

### 新增

- **生成 meta user prompt。** 当对话以包含上下文的 user 内容开始时，生成的 LangGraph 项目可将其保留为渲染后的 meta user prompt，并在运行时注入模型调用。

### 修复

- Available Skills 会实时更新，不再停留在先前解析结果；生成项目也会将空的 Skill 选择视为当前所有已启用 Skills。
- 生成项目安装依赖时加入超时上限和更清晰的恢复说明，超时后不再留下孤立的 `uv` 进程。
- 生成的 `pyproject.toml` 会直接声明支持的依赖版本，并提供可选的中国大陆包镜像说明。

## [4.4.0] - 2026-07-21

现在可以将任意对话转换为可运行 Agent。

### 新增

- **生成可运行 Agent（Beta）。** 可将对话导出为可运行的 LangGraph（Python）项目。分步向导会创建包含模型工厂、渲染后的提示词（变量在运行时保持动态）、内置工具真实代码、自定义函数工具可导入 stub 和 MCP 脚手架的 `uv` 项目；随后写入简短的 `PLAN.md` 供 Coding Agent 完成，并打开项目。项目自带本地 Web UI 和调试器，可通过 `uv run langgraph dev` 启动。

## [4.3.0] - 2026-07-20

提示词模板能力全面升级：支持 Jinja2 逻辑、更丰富的变量，以及指向新功能的应用内提醒。

### 新增

- **Jinja2 提示词模板。** 提示词支持 Jinja2 语法：`{% if %}` / `{% for %}`、字段访问（`{{ user.name }}`）、过滤器（`{{ x | upper }}`），以及用于内联其他文件内容的 `@include("path/to/file")` 宏。
- **新变量类型。** 除普通 **Text** 外，新增 **JSON** 变量（渲染时解析为可分支、可迭代对象，单独使用 `{{ data }}` 会输出格式化 JSON）和 **File content** 变量（通过原生文件选择器选择或输入路径，运行时内联文件内容）。

### 变更

- 模型选择器默认显示参数按钮，并在聚焦时选中模型名称，便于快速编辑。

### 修复

- 外部链接仅允许安全 URL scheme。
- 根目录创建被限制在预期位置内。
- 对话文件采用原子写入，避免中断时损坏。

### 性能

- Trace 侧栏改为延迟加载。
- RPC 流队列现在具有均摊 O(1) 复杂度。

## [4.2.1] - 2026-07-20

小型维护版本：新增一个模型并修复若干问题。

### 新增

- 内置模型新增 **Kimi K3**。

### 修复

- 恢复 Codex CLI 身份验证。
- 对话工作区中的工具和变量列表会在受限高度内滚动，不再撑开布局。

## [4.2.0] - 2026-07-17

现在可以通过链接与任何人分享对话。

### 新增

- **分享对话。** 点击对话工具栏右上角、运行历史旁的 **share icon**，或使用 **File → Share…**，也可以右键文件树中的对话并选择 **Share…**，即可发布任何人都能在浏览器打开的链接。对话会以 **secret GitHub Gist** 发布到你的账户；删除该 Gist 即可撤销访问。
- **GitHub 登录。** 可在 **Settings → Account** 登录 GitHub。分享功能需要登录，因此 Share 对话框也会在首次使用时引导完成登录。

## [4.1.1] - 2026-07-16

聚焦工具导入对话框的小型 UX 版本。

### 变更

- 内置工具与 MCP 工具导入对话框会在侧栏提供按分类或服务器的操作，包括用于快速切换整组工具的 **Enable all** 和 **Disable all**。
- 添加大量工具后，对话工作区中的工具行会滚动显示，不再撑开布局。

## [4.1.0] - 2026-07-16

维护版本：没有重大功能，包含若干问题修复以及三个新的模型/搜索提供商。

### 新增

- 新增 **VolcEngine Agent Plan** 模型提供商及其内置模型。
- 新增 **Brave Search** Web 搜索提供商，可在 Settings → Search 中选择。
- 现有 **Coding Plan** 提供商新增一个模型。

### 修复

- 覆盖对话后会正确合并标签页。
- 重命名对话时不再覆盖同名的已有对话。
- 删除被多个对话引用的提示词变量时会先请求确认。
- 助手消息中的图片内容会被忽略，不再导致错误。
- active-tab ref 的同步移出渲染主体，修复陈旧引用的边界情况。
- Brave Search API 错误会展示给用户。
- 较长的 Skill 描述不再溢出容器。

## [4.0.1]

本更新日志的起始版本。更早的历史请参阅
[GitHub Releases](https://github.com/deer-flow/llm-space/releases)。

[4.2.0]: https://github.com/deer-flow/llm-space/releases/tag/v4.2.0
[4.1.1]: https://github.com/deer-flow/llm-space/releases/tag/v4.1.1
[4.1.0]: https://github.com/deer-flow/llm-space/releases/tag/v4.1.0
[4.0.1]: https://github.com/deer-flow/llm-space/releases/tag/v4.0.1
